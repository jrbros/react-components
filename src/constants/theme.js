import * as COLORS from './colors';

const THEME = {
    button: {
        default: {
            background: COLORS.LIGHTGRAY,
            color: COLORS.GRAY,
            border: 'none'
        }
    }
};

export default THEME;
